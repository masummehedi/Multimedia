<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.seu.mymultimedia">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".player_landscape"
            android:theme="@style/Theme.FullScreen"
            android:screenOrientation="landscape"
            tools:ignore="LockedOrientationActivity"></activity>
        <activity android:name=".Video_list" />
        <activity android:name=".Music_List" />
        <activity android:name=".Image_Viwer" />
        <activity android:name=".Video_Player" />
        <activity android:name=".Music_Player" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>